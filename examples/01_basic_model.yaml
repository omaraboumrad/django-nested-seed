# Basic Model
# Simple objects with primitive fields
#
# Models:
# -------
# class Category(models.Model):
#     name = models.CharField(max_length=100)
#     slug = models.SlugField(unique=True)

testapp:
  Category:
    - name: "Python"
      slug: "python"
    - name: "Django"
      slug: "django"
